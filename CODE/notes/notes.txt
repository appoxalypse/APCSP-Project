use form tag
example:
	<form action="elephant.php" method="post">
		<select name="myChoice">
			<option value="african">African Elephant</option>
			<option value="asian">Asian Elephant</option>
	</form>





login.php
	<php?
		$host
		#username
		$pass
		$db
		$port
	?>


elephant.php
	require_once "login.php";
	include "index.html";
	$connection = mysqli_connect($host, $user, $pass, $db, $port);
	if ($connection->connect_error) die($connection->connect_error);
	$option = isset($_POST["myChoice"]);
	if ($option) {
		$query = "select URL from elephants where elephant =". $POST['myChoice'];

	}